## Summary

- 

## Validation

- [ ] I started this branch from the latest canonical `main` (`upstream/main` for forks or `origin/main` for maintainers).
- [ ] `npm run check:pr -- upstream/main` passes (using `origin/main` when `origin` is canonical).
- [ ] `npm run check` passes.
- [ ] `npm run build:vercel` passes.
- [ ] `npm run security:secrets:history` passes.
- [ ] `npm audit --audit-level=moderate` passes.
- [ ] Existing numbered migrations are unchanged; schema corrections use a new forward-only migration.
- [ ] This PR contains only the new milestone and does not resubmit changes already merged to `main`.
- [ ] User-visible changes include responsive, keyboard, focus, reduced-motion, and accessibility checks where applicable.
- [ ] Setup, behavior, security, and deployment documentation are updated where applicable.

## Security and deployment

- Tenant boundary impact:
- Credentials or new permissions:
- Migration/deployment implications:

## Known limitations and exact next milestone

- 

## Visual evidence (when applicable)

- Not applicable.
