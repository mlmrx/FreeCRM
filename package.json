{
  "name": "free-crm",
  "version": "0.1.0",
  "private": true,
  "description": "A private, open-source CRM operating system for solopreneurs, backed by durable cloud data and document storage.",
  "license": "MIT",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "npm run db:local:migrate && vinext dev",
    "device": "npm run build && npm run db:local:migrate && wrangler dev -c dist/server/wrangler.json --ip 127.0.0.1 --port 3477 --persist-to .wrangler/state --var FREE_CRM_LOCAL_MODE:true",
    "build": "vinext build",
    "start": "vinext start",
    "lint": "eslint app lib server db tests proxy.ts vite.config.ts next.config.ts drizzle.config.ts vitest.config.ts eslint.config.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:db": "python scripts/verify-schema.py",
    "security:secrets": "node scripts/scan-secrets.mjs",
    "security:secrets:history": "node scripts/scan-secrets.mjs --history",
    "smoke:api": "node scripts/smoke-api.mjs",
    "db:local:migrate": "wrangler d1 migrations apply DB --local -c wrangler.local.jsonc --persist-to .wrangler/state",
    "db:cloud:migrate": "wrangler d1 migrations apply DB --remote -c wrangler.jsonc",
    "db:generate": "drizzle-kit generate",
    "db:check": "drizzle-kit check",
    "deploy": "npm run build && npm run db:cloud:migrate && wrangler deploy -c wrangler.jsonc --keep-vars",
    "deploy:cloudflare": "node scripts/deploy-cloudflare.mjs",
    "check": "npm run security:secrets && npm run lint && npm run typecheck && npm run test:coverage && npm run test:db && npm run db:check && npm run build",
    "check:pr": "node scripts/check-pr-base.mjs"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2",
    "jose": "6.2.10",
    "next": "16.3.3",
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.37.1",
    "@cloudflare/workers-types": "4.20260515.1",
    "@openai/sites-vite-plugin": "^0.1.0 || ^0.2.0",
    "@tailwindcss/postcss": "4.2.1",
    "@types/node": "22.19.19",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.2",
    "@vitejs/plugin-rsc": "0.5.26",
    "@vitest/coverage-v8": "4.0.18",
    "drizzle-kit": "0.31.10",
    "eslint": "9.39.4",
    "eslint-config-next": "16.2.6",
    "react-server-dom-webpack": "19.2.6",
    "tailwindcss": "4.2.1",
    "typescript": "5.9.3",
    "vinext": "1.0.0-beta.3",
    "vite": "8.0.13",
    "vitest": "4.0.18",
    "wrangler": "4.92.0"
  },
  "type": "module",
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "Your private D1 relational database for CRM records, audit history, workflows, and reports."
      },
      "FILES": {
        "description": "Your private R2 bucket for workspace-prefixed documents. Do not enable public bucket access."
      },
      "FREE_CRM_AUTH_MODE": {
        "description": "Keep `cloudflare-access`. The app stays sealed until the team domain, audience, and exact owner email are all configured."
      },
      "FREE_CRM_ACCESS_TEAM_DOMAIN": {
        "description": "Your non-secret Cloudflare Access team domain, configured during private activation."
      },
      "FREE_CRM_ACCESS_AUD": {
        "description": "The non-secret audience of the exact Worker Access application."
      },
      "FREE_CRM_OWNER_EMAIL": {
        "description": "The one owner email accepted after Access JWT verification."
      }
    }
  }
}
